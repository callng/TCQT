name: Build TCQT

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - '**.txt'
      - 'renovate.json'
      - '.editorconfig'
      - '.gitignore'
      - '.github/**'
      - '.idea/**'
      - '!.github/workflows/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}-${{ github.event_name }}
  cancel-in-progress: true

jobs:
  build:
    name: Build CI
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    if: >-
      ${{ 
        github.event_name == 'workflow_dispatch' ||
        (
          github.event_name == 'push' &&
          !startsWith(github.event.head_commit.message, '[skip ci]') &&
          github.ref == 'refs/heads/main'
        )
      }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6.3.0
        with:
          build-scan-terms-of-use-url: 'https://gradle.com/terms-of-service'
          build-scan-terms-of-use-agree: 'yes'

      - name: Build with Gradle
        env:
          SIGN_KEYSTORE_BASE64: ${{ secrets.SIGN_KEYSTORE_BASE64 }}
          KEYSTORE_PATH: '../keystore.jks'
          KEYSTORE_PASSWORD: ${{ secrets.SIGN_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.SIGN_ALIAS }}
          KEY_PASSWORD: ${{ secrets.SIGN_KEY_PASSWORD }}
        run: |
          set -euo pipefail

          printf '%s' "$SIGN_KEYSTORE_BASE64" | base64 --decode > keystore.jks

          chmod +x ./gradlew

          ./gradlew \
            :app:packageZygiskModule \
            --stacktrace \
            --build-cache \
            -Dorg.gradle.vfs.watch=false \
            -Dorg.gradle.configuration-cache=false

          ZIP_FILE=$( \
            find app/build/outputs/zygisk \
              -type f \
              -name '*.zip' \
              -print \
              -quit \
          )

          if [ -z "$ZIP_FILE" ] || [ ! -f "$ZIP_FILE" ]; then
            echo "::error::没有找到 Zygisk 模块 ZIP 文件"
            exit 1
          fi

          echo "ZIP_FILE=$ZIP_FILE" >> "$GITHUB_ENV"
          echo "Zygisk 模块 ZIP: $ZIP_FILE"

      - name: Show Artifacts SHA256
        run: |
          set -euo pipefail

          zip_sha256=$(sha256sum "$ZIP_FILE" | cut -d ' ' -f 1)

          {
            echo "### Build Success :rocket:"
            echo
            echo "| Type | SHA256 |"
            echo "|:----:|:-------|"
            echo "| Zygisk Module | \`$zip_sha256\` |"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload Release Artifact
        uses: actions/upload-artifact@v7
        with:
          path: ${{ env.ZIP_FILE }}
          name: TCQT-zygisk-${{ github.sha }}
          if-no-files-found: error

  upload-telegram:
    name: Upload Release
    runs-on: ubuntu-latest
    needs:
      - build
    if: >-
      ${{ 
        success() &&
        github.repository == 'callng/TCQT'
      }}
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Prepare and Upload Release to Telegram
        env:
          COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
          COMMIT_AUTHOR: ${{ github.event.head_commit.author.name }}
          COMMIT_URL: ${{ github.event.head_commit.url }}
          CURRENT_SHA: ${{ github.sha }}
          CURRENT_ACTOR: ${{ github.actor }}
          REPOSITORY_URL: ${{ github.server_url }}/${{ github.repository }}
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
        run: |
          set -euo pipefail

          # download-artifact 已经把 Artifact 内容直接下载到了 artifacts/
          zip_file=$( \
            find artifacts \
              -type f \
              -name 'TCQT-zygisk-*.zip' \
              -print \
              -quit
          )

          echo "Build ZIP: $zip_file"

          if [ -z "$zip_file" ] || [ ! -f "$zip_file" ]; then
            echo "::error::没找到 Zygisk 模块 ZIP 文件"
            exit 1
          fi

          # 重命名为 .apk
          apk_file="${zip_file%.zip}.apk"
          mv "$zip_file" "$apk_file"

          if [ ! -f "$apk_file" ]; then
            echo "::error::重命名失败: $apk_file"
            exit 1
          fi

          # 验证 APK 实际上仍然是合法 ZIP
          if ! unzip -t "$apk_file" >/dev/null 2>&1; then
            echo "::error::重命名后的 APK 不是有效 ZIP/APK"
            exit 1
          fi

          echo "Final APK: $apk_file"
          echo "Size: $(du -h "$apk_file" | cut -f1)"
          echo "SHA256: $(sha256sum "$apk_file" | cut -d ' ' -f1)"
          file "$apk_file"

          python3 > caption.txt <<'PY'
          import html
          import os

          message = os.environ.get("COMMIT_MESSAGE", "").strip()
          author = os.environ.get("COMMIT_AUTHOR", "").strip()
          commit_url = os.environ.get("COMMIT_URL", "").strip()

          current_sha = os.environ.get("CURRENT_SHA", "").strip()
          current_actor = os.environ.get("CURRENT_ACTOR", "").strip()
          repository_url = os.environ.get("REPOSITORY_URL", "").rstrip("/")

          if not message:
              message = f"Manual build for commit {current_sha}"

          if not author:
              author = current_actor

          if not commit_url:
              commit_url = f"{repository_url}/commit/{current_sha}"

          if len(message) > 700:
              message = message[:697] + "..."

          escaped_message = html.escape(message)
          escaped_author = html.escape(author)
          escaped_url = html.escape(commit_url, quote=True)

          caption = (
              "New push to GitHub!\n"
              f"<pre>{escaped_message}</pre>\n"
              f"by <code>{escaped_author}</code>\n"
              f'See commit detail <a href="{escaped_url}">here</a>'
          )

          print(caption)
          PY

          echo "Telegram caption:"
          cat caption.txt
          echo

          MAX_RETRIES=5
          RETRY_DELAY=5

          for attempt in $(seq 1 "$MAX_RETRIES"); do
            echo "Attempt $attempt/$MAX_RETRIES..."

            : > response.json

            http_code=$( \
              curl \
                --silent \
                --show-error \
                --output response.json \
                --write-out '%{http_code}' \
                --form-string "chat_id=${CHANNEL_ID}" \
                --form "document=@${apk_file};type=application/vnd.android.package-archive" \
                --form "caption=<caption.txt" \
                --form-string "parse_mode=HTML" \
                "https://api.telegram.org/bot${BOT_TOKEN}/sendDocument"
            ) || http_code='000'

            echo "HTTP Status: $http_code"
            echo "Response:"

            if [ -s response.json ]; then
              cat response.json
            else
              echo '<empty response>'
            fi

            echo

            if [ "$http_code" = '200' ]; then
              if python3 - response.json <<'PY'
          import json
          import sys

          response_file = sys.argv[1]

          try:
              with open(response_file, "r", encoding="utf-8") as file:
                  response = json.load(file)
          except Exception as error:
              print(f"Unable to parse Telegram response: {error}")
              sys.exit(1)

          if response.get("ok") is True:
              sys.exit(0)

          print(f"Telegram API returned ok=false: {response}")
          sys.exit(1)
          PY
              then
                echo "Upload succeeded!"
                exit 0
              fi

              echo "::error::Telegram API 返回 ok=false"
              exit 1
            fi

            case "$http_code" in
              429|5??|000)
                if [ "$attempt" -lt "$MAX_RETRIES" ]; then
                  echo "Temporary request failure."
                  echo "Retrying in ${RETRY_DELAY}s..."

                  sleep "$RETRY_DELAY"
                  RETRY_DELAY=$((RETRY_DELAY * 2))
                fi
                ;;

              *)
                echo "::error::Telegram request failed with HTTP $http_code"
                exit 1
                ;;
            esac
          done

          echo "::error::All $MAX_RETRIES attempts failed"
          exit 1